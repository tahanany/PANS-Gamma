faktor = Calculates the factorial of a given number using a simple iterative for loop.<p>

faktortest = Unit tests for the factorial function using Python's unittest framework to ensure code reliability.<p>

power = Defines a function to calculate the power of a number using the ** operator.<p>

test = Unit tests for the custom exponentiation function, verifying edge cases like negative exponents.<p>

triangle = Applies the Triangle Inequality Theorem to check if a valid triangle can be formed.<p>
