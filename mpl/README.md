"Modern Programm�ng Languages" <p>

belowten = Calculates the sum of all multiples of 3 or 5 below 1000 (Project Euler Problem 1).<p>

smallthree = Generates three random numbers and calculates the smallest value among them.<p>

bigthree = Generates three random numbers and calculates the biggest value among them.<p>

dices = Performs a Monte Carlo simulation by rolling two dice 1 million times to analyze the probability distribution of their sums.<p>

faktor = Calculates the factorial of a given number using a simple iterative for loop.<p>

faktortest = Implements unit tests using Python's unittest framework to verify the accuracy of the factorial function.<p>

power = Defines a function to calculate the power of a number (exponentiation) using the ** operator.<p>

probability = A Monte Carlo simulation (revisited) that analyzes the probability distribution of the sum of two dice rolling 1 million times.<p>

randomlist = Demonstrates basic list manipulation by generating random numbers, appending items, and calculating the rounded average.<p>

reversed = Demonstrates iterating through a list in both forward and reverse directions using for loops and index manipulation.<p>

triangle = Determines whether a triangle can be formed given three side lengths by applying the Triangle Inequality Theorem.<p>
